/**
 * Metodos con string = includes, length
 */

const producto="Monitor de 20 pulgadas";

console.log(producto);
//devuelve la posicion del contenido en la cadena, si no existe devuelve -1
console.log(producto.indexOf('des'));
//cantidad de caracteres de la cadena de texto 
console.log(producto.length);
//devuelve true o falso si existe el contenido, es sencible a mayusculas
console.log(producto.includes('Monitor'));